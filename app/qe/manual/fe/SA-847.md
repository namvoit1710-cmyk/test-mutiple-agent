# Unit Test Cases: SA-847 [FE]

Source spec: `app/docs/spec/SA-847.md`

## Module: LoginForm

File: `app/fe/src/components/LoginForm.tsx`

| TC | Description | Input | Expected Output |
|----|-------------|-------|-----------------|
| TC-001 | Verify "Login" heading is rendered | Initial render | `<h2>Login</h2>` is visible |
| TC-002 | Verify "Username" label and text input are rendered | Initial render | `<label>Username</label>` and `<input type="text">` are visible |
| TC-003 | Verify "Password" label and password input are rendered | Initial render | `<label>Password</label>` and `<input type="password">` are visible |
| TC-004 | Verify "Login" submit button is rendered | Initial render | `<button type="submit">Login</button>` is visible |
| TC-005 | Verify typing in username field updates state | User types "alice" into username field | `username` state is "alice", input value is "alice" |
| TC-006 | Verify typing in password field updates state | User types "password123" into password field | `password` state is "password123", input value is "password123" |
| TC-007 | Verify submit button prevents default form submission | Click submit button | `event.preventDefault()` is called, no page reload occurs |
| TC-008 | Verify submit button displays alert with entered credentials | `username="alice"`, `password="password123"`, click submit | `window.alert` called with "Username: alice, Password: password123" |
| TC-009 | Verify submit button displays alert with empty fields | `username=""`, `password=""`, click submit | `window.alert` called with "Username: , Password: " |

## Module: App

File: `app/fe/src/App.tsx`

| TC | Description | Input | Expected Output |
|----|-------------|-------|-----------------|
| TC-010 | Verify LoginForm component is rendered in App | Initial render | `<LoginForm />` is rendered in the DOM |

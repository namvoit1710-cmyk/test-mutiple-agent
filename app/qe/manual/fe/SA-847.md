# Unit Test Cases: SA-847 [FE]

Source spec: `app/docs/spec/SA-847.md`

## Module: LoginForm

File: `app/fe/src/components/LoginForm.tsx`

| TC | Description | Input | Expected Output |
|----|-------------|-------|-----------------|
| TC-001 | Verify "Login" heading is rendered | None | `<h2>Login</h2>` is present |
| TC-002 | Verify username field is rendered with correct label | None | `<label>Username</label>` and `<input type="text" id="username">` are present |
| TC-003 | Verify password field is rendered with correct label and type | None | `<label>Password</label>` and `<input type="password" id="password">` are present |
| TC-004 | Verify submit button is rendered | None | `<button type="submit">Login</button>` is present |
| TC-005 | Verify username input updates state | `input value = "testuser"` | `username` state is `"testuser"` |
| TC-006 | Verify password input updates state | `input value = "password123"` | `password` state is `"password123"` |
| TC-007 | Verify form submission prevents default behavior | `submit event` | `event.preventDefault()` is called |
| TC-008 | Verify submit alert displays correct values for provided credentials | `username = "alice", password = "secretPassword"` | `window.alert` called with `"Username: alice, Password: secretPassword"` |
| TC-009 | Verify submit alert displays correct values for empty fields | `username = "", password = ""` | `window.alert` called with `"Username: , Password: "` |

## Module: App

File: `app/fe/src/App.tsx`

| TC | Description | Input | Expected Output |
|----|-------------|-------|-----------------|
| TC-010 | Verify LoginForm is rendered in App | None | `<LoginForm />` is present in the DOM |

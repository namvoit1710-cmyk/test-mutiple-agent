# Unit Test Cases: SA-847 [FE]

Source spec: `app/docs/spec/SA-847.md`

## Module: LoginForm

File: `app/fe/src/components/LoginForm.tsx`

| TC | Description | Input | Expected Output |
|----|-------------|-------|-----------------|
| TC-001 | Verify "Login" heading is rendered | Component renders | `<h2>Login</h2>` is present |
| TC-002 | Verify username field is rendered | Component renders | `<label htmlFor="username">Username</label>` and `<input id="username" type="text" />` are present |
| TC-003 | Verify password field is rendered | Component renders | `<label htmlFor="password">Password</label>` and `<input id="password" type="password" />` are present |
| TC-004 | Verify submit button is rendered | Component renders | `<button type="submit">Login</button>` is present |
| TC-005 | Verify username state updates on change | `username` input value changes to `"john_doe"` | `username` state is `"john_doe"` |
| TC-006 | Verify password state updates on change | `password` input value changes to `"secret123"` | `password` state is `"secret123"` |
| TC-007 | Verify form submission prevents page reload | Submit button clicked | `event.preventDefault()` is called |
| TC-008 | Verify alert displays correct credentials on submit | `username="alice"`, `password="password123"`, Submit clicked | `window.alert` called with `"Username: alice, Password: password123"` |
| TC-009 | Verify alert displays empty credentials on submit | `username=""`, `password=""`, Submit clicked | `window.alert` called with `"Username: , Password: "` |

## Module: App

File: `app/fe/src/App.tsx`

| TC | Description | Input | Expected Output |
|----|-------------|-------|-----------------|
| TC-010 | Verify LoginForm is rendered in App | Component renders | `<LoginForm />` is present in the DOM |

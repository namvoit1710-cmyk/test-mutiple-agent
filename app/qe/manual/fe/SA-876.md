# Unit Test Cases: SA-876 [FE]

Source spec: `app/docs/spec/SA-876.md`

## Module: PasswordControl

File: `app/fe/src/components/password-control.tsx`

| TC | Description | Input | Expected Output |
|----|-------------|-------|-----------------|
| TC-001 | Renders with default props | `<PasswordControl />` | Input type is "password", toggle button aria-label is "Show password" |
| TC-002 | Renders with placeholder prop | `<PasswordControl placeholder="Enter password" />` | Input element has placeholder "Enter password" |
| TC-003 | Renders with disabled prop | `<PasswordControl disabled={true} />` | Input element is disabled, toggle button is disabled |
| TC-004 | Renders with className prop | `<PasswordControl className="custom-class" />` | Wrapper div has class "custom-class" |
| TC-005 | Renders with id prop | `<PasswordControl id="my-password" />` | Input element has id "my-password" |
| TC-006 | Renders with name prop | `<PasswordControl name="password-field" />` | Input element has name "password-field" |
| TC-007 | Renders with autoComplete prop | `<PasswordControl autoComplete="new-password" />` | Input element has autoComplete "new-password" |
| TC-008 | Toggle button switches input type from password to text | Render `<PasswordControl />`, click toggle button | Input type is "text", toggle button aria-label is "Hide password" |
| TC-009 | Toggle button switches input type from text to password | Render `<PasswordControl />`, click toggle button twice | Input type is "password", toggle button aria-label is "Show password" |
| TC-010 | Toggle button has type="button" | `<PasswordControl />` | Toggle button element has attribute type="button" |
| TC-011 | Toggle button has tabindex="0" | `<PasswordControl />` | Toggle button element has attribute tabindex="0" |
| TC-012 | Toggle button is disabled when component is disabled | `<PasswordControl disabled={true} />` | Toggle button element is disabled |
| TC-013 | `getValue()` returns current input value | Render `<PasswordControl />`, set input value to "test1234", call `ref.current.getValue()` | Returns "test1234" |
| TC-014 | `setValue()` sets input value | Render `<PasswordControl />`, call `ref.current.setValue("newpass")` | Input element value is "newpass" |
| TC-015 | `clear()` clears input value and resets visibility | Render `<PasswordControl />`, set input value to "test", toggle visibility, call `ref.current.clear()` | Input element value is "", input type is "password" |
| TC-016 | Wrapper div has class `.password-control` | `<PasswordControl />` | Rendered wrapper div has class "password-control" |
| TC-017 | Input element has class `.password-control__input` | `<PasswordControl />` | Rendered input element has class "password-control__input" |
| TC-018 | Toggle button has class `.password-control__toggle` | `<PasswordControl />` | Rendered toggle button has class "password-control__toggle" |
| TC-019 | Initial state shows eye icon | `<PasswordControl />` | Eye icon SVG is visible, eye-off icon SVG is hidden |
| TC-020 | Toggling shows eye-off icon | Render `<PasswordControl />`, click toggle button | Eye-off icon SVG is visible, eye icon SVG is hidden |
| TC-021 | Toggling back shows eye icon | Render `<PasswordControl />`, click toggle button twice | Eye icon SVG is visible, eye-off icon SVG is hidden |

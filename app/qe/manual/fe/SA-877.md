# Unit Test Cases: SA-877 [FE]

Source spec: `app/docs/spec/SA-877.md`

## Module: Input

File: `app/fe/src/components/input.tsx`

| TC | Description | Input | Expected Output |
|----|-------------|-------|-----------------|
| TC-001 | Renders an input element with default type "text" and empty value. | `<Input />` | `<input type="text" value="" />` |
| TC-002 | Renders an input element with the `.input` class applied. | `<Input />` | `<input class="input" />` |
| TC-003 | Renders with a provided `value`. | `<Input value="test" />` | `<input value="test" />` |
| TC-004 | Renders with an empty `value`. | `<Input value="" />` | `<input value="" />` |
| TC-005 | Calls `onChange` with the new value when input changes. | `<Input onChange={mockOnChange} />` and simulate change event with `event.target.value = "new value"` | `mockOnChange` called with `"new value"` |
| TC-006 | Renders with a provided `placeholder`. | `<Input placeholder="Enter text" />` | `<input placeholder="Enter text" />` |
| TC-007 | Renders without a `placeholder` when not provided. | `<Input />` | `<input>` element without `placeholder` attribute |
| TC-008 | Renders a disabled input when `disabled` is true. | `<Input disabled={true} />` | `<input disabled />` |
| TC-009 | Renders an enabled input when `disabled` is false. | `<Input disabled={false} />` | `<input>` element without `disabled` attribute |
| TC-010 | Renders with a provided `type` (e.g., "password"). | `<Input type="password" />` | `<input type="password" />` |
| TC-011 | Renders with default `type` "text" when not provided. | `<Input />` | `<input type="text" />` |
| TC-012 | Applies an additional `className`. | `<Input className="custom-class" />` | `<input class="input custom-class" />` |
| TC-013 | Renders with a provided `id`. | `<Input id="my-input" />` | `<input id="my-input" />` |
| TC-014 | Renders with a provided `name`. | `<Input name="my-field" />` | `<input name="my-field" />` |

## Module: input.css styles

File: `app/fe/src/components/input.css`

| TC | Description | Input | Expected Output |
|----|-------------|-------|-----------------|
| TC-015 | Verifies `border: 1px solid red` is applied. | Rendered `<Input />` component | Computed style `border` is `1px solid red` |
| TC-016 | Verifies `border-radius: 16px` is applied. | Rendered `<Input />` component | Computed style `border-radius` is `16px` |
| TC-017 | Verifies `padding: 8px 12px` is applied. | Rendered `<Input />` component | Computed style `padding` is `8px 12px` |
| TC-018 | Verifies `outline: none` is applied. | Rendered `<Input />` component | Computed style `outline` is `none` |
| TC-019 | Verifies `font-size: 14px` is applied. | Rendered `<Input />` component | Computed style `font-size` is `14px` |
| TC-020 | Verifies `width: 100%` is applied. | Rendered `<Input />` component | Computed style `width` is `100%` |

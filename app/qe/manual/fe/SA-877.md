# Unit Test Cases: SA-877 [FE]

Source spec: `app/docs/spec/SA-877.md`

## Module: Input

File: `app/fe/src/components/input.tsx`

| TC | Description | Input | Expected Output |
|----|-------------|-------|-----------------|
| TC-001 | Renders with default props | `{}` | An `<input>` element with `type="text"`, `value=""`, `placeholder=""`, not disabled, and class `input`. |
| TC-002 | Renders with a given value | `{ value: "test" }` | An `<input>` element with `value="test"`. |
| TC-003 | Calls onChange with new value on input change | `{ onChange: (val) => {} }` | `onChange` is called with the new input value. |
| TC-004 | Renders with a placeholder | `{ placeholder: "Enter text" }` | An `<input>` element with `placeholder="Enter text"`. |
| TC-005 | Renders as disabled | `{ disabled: true }` | An `<input>` element with the `disabled` attribute. |
| TC-006 | Renders with a different type | `{ type: "password" }` | An `<input>` element with `type="password"`. |
| TC-007 | Applies custom className | `{ className: "custom-class" }` | An `<input>` element with classes `input custom-class`. |
| TC-008 | Applies id attribute | `{ id: "my-input" }` | An `<input>` element with `id="my-input"`. |
| TC-009 | Applies name attribute | `{ name: "my-field" }` | An `<input>` element with `name="my-field"`. |
| TC-010 | Handles empty string for value | `{ value: "" }` | An `<input>` element with `value=""`. |
| TC-011 | Handles undefined value prop | `{ value: undefined }` | An `<input>` element with `value=""` (default). |
| TC-012 | Handles null value prop | `{ value: null }` | An `<input>` element with `value=""` (default). |
| TC-013 | Handles no onChange prop | `{}` | Input value can be changed without error. |

## Module: Input Styles

File: `app/fe/src/components/input.css`

| TC | Description | Input | Expected Output |
|----|-------------|-------|-----------------|
| TC-014 | Input has red border | Rendered `Input` component | `border: 1px solid red` |
| TC-015 | Input has 16px border-radius | Rendered `Input` component | `border-radius: 16px` |
| TC-016 | Input has correct padding | Rendered `Input` component | `padding: 8px 12px` |
| TC-017 | Input has no outline | Rendered `Input` component | `outline: none` |
| TC-018 | Input has correct font-size | Rendered `Input` component | `font-size: 14px` |
| TC-019 | Input has 100% width | Rendered `Input` component | `width: 100%` |
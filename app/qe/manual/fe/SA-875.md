# Unit Test Cases: SA-875 [FE]

Source spec: `app/docs/spec/SA-875.md`

## Module: AuthContext

File: `app/fe/src/lib/auth-context.tsx`

| TC | Description | Input | Expected Output |
|----|-------------|-------|-----------------|
| TC-001 | `login` sets user and authentication state | `email = "user@example.com", password = "password123"` | `user = { email: "user@example.com", password: "password123" }, isAuthenticated = true` |
| TC-002 | `logout` clears user and authentication state | `user = { email: "user@example.com", password: "password123" }` | `user = null, isAuthenticated = false` |
| TC-003 | `useAuth` throws error when used outside `AuthProvider` | `useAuth()` called in component without `AuthProvider` parent | `Error: useAuth must be used within an AuthProvider` |

## Module: PrivateRoute

File: `app/fe/src/components/private-route.tsx`

| TC | Description | Input | Expected Output |
|----|-------------|-------|-----------------|
| TC-004 | Renders children when authenticated | `isAuthenticated = true` | `children` are rendered |
| TC-005 | Redirects to login when not authenticated | `isAuthenticated = false` | `<Navigate to="/login" replace>` is rendered |

## Module: AuthRoute

File: `app/fe/src/components/auth-route.tsx`

| TC | Description | Input | Expected Output |
|----|-------------|-------|-----------------|
| TC-006 | Renders children when not authenticated | `isAuthenticated = false` | `children` are rendered |
| TC-007 | Redirects to home when authenticated | `isAuthenticated = true` | `<Navigate to="/home" replace>` is rendered |

## Module: PasswordControl

File: `app/fe/src/components/password-control.tsx`

| TC | Description | Input | Expected Output |
|----|-------------|-------|-----------------|
| TC-008 | Initial state renders password input and eye icon | `value = "secret"` | `input type="password"`, eye icon visible |
| TC-009 | Clicking toggle shows password and eye-off icon | `click toggle button` | `input type="text"`, eye-off icon visible |
| TC-010 | Clicking toggle again hides password and shows eye icon | `click toggle button` (while visible) | `input type="password"`, eye icon visible |
| TC-011 | Disabled prop disables both input and toggle | `disabled = true` | `input` and `button` both have `disabled` attribute |
| TC-012 | Input change triggers onChange callback | `input value change to "newpass"` | `onChange("newpass")` is called |

## Module: LoginPage

File: `app/fe/src/pages/login.tsx`

| TC | Description | Input | Expected Output |
|----|-------------|-------|-----------------|
| TC-013 | Renders all required form elements | `initial render` | Email input, PasswordControl, and Submit button are visible |
| TC-014 | Submit with empty email shows validation error | `email = "", password = "password123"` | `"Email is required"` error message is visible |
| TC-015 | Submit with invalid email format shows validation error | `email = "invalid-email", password = "password123"` | `"Invalid email format"` error message is visible |
| TC-016 | Submit with empty password shows validation error | `email = "user@example.com", password = ""` | `"Password is required"` error message is visible |
| TC-017 | Valid submit calls login function | `email = "user@example.com", password = "password123"` | `login("user@example.com", "password123")` is called |

## Module: HomePage

File: `app/fe/src/pages/home.tsx`

| TC | Description | Input | Expected Output |
|----|-------------|-------|-----------------|
| TC-018 | Renders welcome greeting with user email | `user = { email: "user@example.com" }` | `"Welcome, user@example.com"` is visible |
| TC-019 | Renders the application slogan | `initial render` | `"Intelligent automation starts here."` is visible |
| TC-020 | Clicking logout button calls logout function | `click logout button` | `logout()` is called |

## Module: App

File: `app/fe/src/App.tsx`

| TC | Description | Input | Expected Output |
|----|-------------|-------|-----------------|
| TC-021 | Renders core providers | `initial render` | `AuthProvider` and `BrowserRouter` are present in tree |
| TC-022 | Login route is protected by AuthRoute | `path = "/login"` | `AuthRoute` wraps `LoginPage` |
| TC-023 | Home route is protected by PrivateRoute | `path = "/home"` | `PrivateRoute` wraps `HomePage` |

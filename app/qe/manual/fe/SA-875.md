# Unit Test Cases: SA-875 [FE]

Source spec: `app/docs/spec/SA-875.md`

## Module: AuthContext

File: `app/fe/src/lib/auth-context.tsx`

| TC | Description | Input | Expected Output |
|----|-------------|-------|-----------------|
| TC-001 | `useAuth` hook returns `isAuthenticated` as `false` initially | N/A | `isAuthenticated` is `false` |
| TC-002 | `login()` sets `isAuthenticated` to `true` | Call `login()` | `isAuthenticated` is `true` |
| TC-003 | `logout()` sets `isAuthenticated` to `false` | Call `login()`, then `logout()` | `isAuthenticated` is `false` |
| TC-004 | `login()` followed by `logout()` results in `isAuthenticated` being `false` | Call `login()`, then `logout()` | `isAuthenticated` is `false` |

## Module: PrivateRoute

File: `app/fe/src/components/private-route.tsx`

| TC | Description | Input | Expected Output |
|----|-------------|-------|-----------------|
| TC-005 | `PrivateRoute` redirects to `/login` when not authenticated | `isAuthenticated = false`, children = `<p>Protected Content</p>` | `<Navigate to="/login" replace />` is rendered |
| TC-006 | `PrivateRoute` renders children when authenticated | `isAuthenticated = true`, children = `<p>Protected Content</p>` | `<p>Protected Content</p>` is rendered |

## Module: AuthRoute

File: `app/fe/src/components/auth-route.tsx`

| TC | Description | Input | Expected Output |
|----|-------------|-------|-----------------|
| TC-007 | `AuthRoute` redirects to `/home` when authenticated | `isAuthenticated = true`, children = `<p>Auth Content</p>` | `<Navigate to="/home" replace />` is rendered |
| TC-008 | `AuthRoute` renders children when not authenticated | `isAuthenticated = false`, children = `<p>Auth Content</p>` | `<p>Auth Content</p>` is rendered |

## Module: HomePage

File: `app/fe/src/pages/home.tsx`

| TC | Description | Input | Expected Output |
|----|-------------|-------|-----------------|
| TC-009 | `HomePage` component renders "Home Page" | N/A | Text "Home Page" is visible |

## Module: LoginPage

File: `app/fe/src/pages/login.tsx`

| TC | Description | Input | Expected Output |
|----|-------------|-------|-----------------|
| TC-010 | `LoginPage` component renders "Login Page" | N/A | Text "Login Page" is visible |
| TC-011 | `LoginPage` button click calls `login()` | Click login button | `login()` function is called, `isAuthenticated` becomes `true` |

## Module: App

File: `app/fe/src/App.tsx`

| TC | Description | Input | Expected Output |
|----|-------------|-------|-----------------|
| TC-012 | No auth → navigate to `/home` | Initial state: `isAuthenticated = false`, navigate to `/home` | Redirected to `/login` |
| TC-013 | Authenticated → navigate to `/login` | Initial state: `isAuthenticated = true`, navigate to `/login` | Redirected to `/home` |
| TC-014 | No auth → navigate to `/login` | Initial state: `isAuthenticated = false`, navigate to `/login` | `LoginPage` renders (stays on `/login`) |
| TC-015 | Authenticated → navigate to `/home` | Initial state: `isAuthenticated = true`, navigate to `/home` | `HomePage` renders (stays on `/home`) |
| TC-016 | Default route `/` when not authenticated | Initial state: `isAuthenticated = false`, navigate to `/` | Redirected to `/login` |
| TC-017 | Default route `/` when authenticated | Initial state: `isAuthenticated = true`, navigate to `/` | Redirected to `/home` |
# Unit Test Cases: SA-875 [FE]

Source spec: `app/docs/spec/SA-875.md`

## Module: Auth Context

File: `app/fe/src/lib/auth-context.tsx`

| TC | Description | Input | Expected Output |
|----|-------------|-------|-----------------|
| TC-001 | `login` sets user and authentication state | `email = "test@example.com", password = "password123"` | `isAuthenticated = true`, `user = { email: "test@example.com", password: "password123" }` |
| TC-002 | `logout` clears user and authentication state | `isAuthenticated = true, user = { email: "test@example.com", ... }` | `isAuthenticated = false`, `user = null` |
| TC-003 | `useAuth` throws error when used outside provider | `useAuth()` called without `AuthProvider` wrapper | `Error: useAuth must be used within an AuthProvider` |

## Module: Private Route

File: `app/fe/src/components/private-route.tsx`

| TC | Description | Input | Expected Output |
|----|-------------|-------|-----------------|
| TC-004 | Renders children when authenticated | `isAuthenticated = true` | `children` are rendered |
| TC-005 | Redirects to login when not authenticated | `isAuthenticated = false` | `<Navigate to="/login" replace>` is rendered |

## Module: Auth Route

File: `app/fe/src/components/auth-route.tsx`

| TC | Description | Input | Expected Output |
|----|-------------|-------|-----------------|
| TC-006 | Renders children when not authenticated | `isAuthenticated = false` | `children` are rendered |
| TC-007 | Redirects to home when authenticated | `isAuthenticated = true` | `<Navigate to="/home" replace>` is rendered |

## Module: Password Control

File: `app/fe/src/components/password-control.tsx`

| TC | Description | Input | Expected Output |
|----|-------------|-------|-----------------|
| TC-008 | Initial state is masked | `value = "secret123"` | `input type="password"`, eye icon is visible |
| TC-009 | Toggle visibility shows password | Click toggle button | `input type="text"`, eye-off icon is visible |
| TC-010 | Toggle visibility hides password | Click toggle button (while visible) | `input type="password"`, eye icon is visible |
| TC-011 | Component is disabled | `disabled = true` | `input` and `button` both have `disabled` attribute |
| TC-012 | Input change triggers onChange | `input value change to "newpass"` | `onChange("newpass")` is called |
| TC-013 | Placeholder is rendered | `placeholder = "Enter password"` | `input` has `placeholder="Enter password"` |

## Module: Home Page

File: `app/fe/src/pages/home.tsx`

| TC | Description | Input | Expected Output |
|----|-------------|-------|-----------------|
| TC-014 | Displays welcome greeting with user email | `user = { email: "user@test.com", ... }` | `"Welcome, user@test.com"` is rendered |
| TC-015 | Displays the correct slogan | `SLOGAN = "Intelligent automation starts here."` | `"Intelligent automation starts here."` is rendered |
| TC-016 | Logout button triggers logout | Click logout button | `logout()` function from auth context is called |

## Module: Login Page

File: `app/fe/src/pages/login.tsx`

| TC | Description | Input | Expected Output |
|----|-------------|-------|-----------------|
| TC-017 | Renders required form fields | Component mount | Email input and `PasswordControl` are rendered |
| TC-018 | Validation error for empty email | `email = ""` | `"Email is required"` error message is visible |
| TC-019 | Validation error for invalid email format | `email = "invalid-email"` | `"Invalid email format"` error message is visible |
| TC-020 | Validation error for empty password | `password = ""` | `"Password is required"` error message is visible |
| TC-021 | Valid submission calls login | `email = "test@test.com", password = "password123"` | `login("test@test.com", "password123")` is called |

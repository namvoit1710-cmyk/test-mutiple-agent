# Manual Test Cases: SA-875 [FE]

Source spec: `app/docs/spec/SA-875.md`

### TC-001: Unauthenticated user attempts to access protected `/home` route

- **Priority:** Critical
- **Type:** Happy Case
- **Preconditions:** The user is not authenticated. The application is running.
- **Test Data:** N/A

| # | Step | Expected Result |
|---|------|-----------------|
| 1 | Navigate to the `/home` URL. | The user is redirected to the `/login` page. |

- **Postconditions:** The user is on the `/login` page.

### TC-002: Authenticated user attempts to access authentication `/login` route

- **Priority:** Critical
- **Type:** Happy Case
- **Preconditions:** The user is authenticated. The application is running.
- **Test Data:** N/A

| # | Step | Expected Result |
|---|------|-----------------|
| 1 | Navigate to the `/login` URL. | The user is redirected to the `/home` page. |

- **Postconditions:** The user is on the `/home` page.

### TC-003: Unauthenticated user accesses authentication `/login` route

- **Priority:** High
- **Type:** Happy Case
- **Preconditions:** The user is not authenticated. The application is running.
- **Test Data:** N/A

| # | Step | Expected Result |
|---|------|-----------------|
| 1 | Navigate to the `/login` URL. | The `/login` page renders successfully, and the user remains on the `/login` page. |

- **Postconditions:** The user is on the `/login` page.

### TC-004: Authenticated user accesses protected `/home` route

- **Priority:** High
- **Type:** Happy Case
- **Preconditions:** The user is authenticated. The application is running.
- **Test Data:** N/A

| # | Step | Expected Result |
|---|------|-----------------|
| 1 | Navigate to the `/home` URL. | The `/home` page renders successfully, and the user remains on the `/home` page. |

- **Postconditions:** The user is on the `/home` page.

### TC-005: Authenticated user logs out and attempts to access protected `/home` route

- **Priority:** High
- **Type:** Negative Case
- **Preconditions:** The user is authenticated. The application is running.
- **Test Data:** N/A

| # | Step | Expected Result |
|---|------|-----------------|
| 1 | Log in the user (simulate `login()` call). | User is authenticated. |
| 2 | Navigate to the `/home` URL. | The `/home` page renders. |
| 3 | Log out the user (simulate `logout()` call). | User is no longer authenticated. |
| 4 | Attempt to navigate back to the `/home` URL (e.g., using browser back button or direct URL). | The user is redirected to the `/login` page. |

- **Postconditions:** The user is on the `/login` page and is not authenticated.

### TC-006: Unauthenticated user attempts to access protected `/home` route after failing to log in

- **Priority:** Medium
- **Type:** Negative Case
- **Preconditions:** The user is not authenticated. The application is running.
- **Test Data:** N/A

| # | Step | Expected Result |
|---|------|-----------------|
| 1 | Attempt to navigate to the `/home` URL. | The user is redirected to the `/login` page. |
| 2 | On the `/login` page, do not perform any login action. | The user remains on the `/login` page. |
| 3 | Attempt to navigate to the `/home` URL again (e.g., by typing the URL). | The user is redirected to the `/login` page. |

- **Postconditions:** The user is on the `/login` page and is not authenticated.

# Unit Test Cases: SA-875 [FE]

Source spec: `app/docs/spec/SA-875.md`

## Module: Auth Context

File: `app/fe/src/lib/auth-context.tsx`

| TC | Description | Input | Expected Output |
|----|-------------|-------|-----------------|
| TC-001 | `login` sets user state and authentication status | `email = "user@example.com", password = "password123"` | `isAuthenticated = true, user = { email: "user@example.com", password: "password123" }` |
| TC-002 | `logout` clears user state and authentication status | `user = { email: "user@example.com", password: "password123" }` | `isAuthenticated = false, user = null` |
| TC-003 | `useAuth` throws error when used outside of `AuthProvider` | `useAuth()` called in component without `AuthProvider` wrapper | `Error: useAuth must be used within an AuthProvider` |

## Module: Private Route

File: `app/fe/src/components/private-route.tsx`

| TC | Description | Input | Expected Output |
|----|-------------|-------|-----------------|
| TC-004 | Renders children when user is authenticated | `isAuthenticated = true` | `children` are rendered |
| TC-005 | Redirects to login page when user is not authenticated | `isAuthenticated = false` | `<Navigate to="/login" replace />` is rendered |

## Module: Auth Route

File: `app/fe/src/components/auth-route.tsx`

| TC | Description | Input | Expected Output |
|----|-------------|-------|-----------------|
| TC-006 | Renders children when user is not authenticated | `isAuthenticated = false` | `children` are rendered |
| TC-007 | Redirects to home page when user is authenticated | `isAuthenticated = true` | `<Navigate to="/home" replace />` is rendered |

## Module: Password Control

File: `app/fe/src/components/password-control.tsx`

| TC | Description | Input | Expected Output |
|----|-------------|-------|-----------------|
| TC-008 | Initial state renders as password input with show icon | `value = "secret"` | `input type="password"`, eye icon visible |
| TC-009 | Clicking toggle switches to text input and hide icon | `click toggle button` | `input type="text"`, eye-off icon visible |
| TC-010 | Clicking toggle again switches back to password input | `click toggle button` (from text state) | `input type="password"`, eye icon visible |
| TC-011 | Disabling component disables both input and toggle | `disabled = true` | `input` and `button` both have `disabled` attribute |
| TC-012 | Input change triggers `onChange` callback | `type "a" into input` | `onChange("a")` is called |

## Module: Home Page

File: `app/fe/src/pages/home.tsx`

| TC | Description | Input | Expected Output |
|----|-------------|-------|-----------------|
| TC-013 | Displays welcome greeting with user email | `user = { email: "test@example.com" }` | `"Welcome, test@example.com"` is visible |
| TC-014 | Displays the correct application slogan | `SLOGAN = "Intelligent automation starts here."` | `"Intelligent automation starts here."` is visible |
| TC-015 | Clicking logout button triggers auth logout | `click logout button` | `logout()` function from context is called |

## Module: Login Page

File: `app/fe/src/pages/login.tsx`

| TC | Description | Input | Expected Output |
|----|-------------|-------|-----------------|
| TC-016 | Renders email input and password control | `initial render` | `input[type="email"]` and `PasswordControl` are visible |
| TC-017 | Shows validation error for empty email on submit | `email = "", password = "password123", submit form` | `"Valid email is required"` error message is visible |
| TC-018 | Shows validation error for invalid email format on submit | `email = "invalid-email", password = "password123", submit form` | `"Valid email is required"` error message is visible |
| TC-019 | Shows validation error for empty password on submit | `email = "test@example.com", password = "", submit form` | `"Password is required"` error message is visible |
| TC-020 | Calls login function with credentials on valid submit | `email = "test@example.com", password = "password123", submit form` | `login("test@example.com", "password123")` is called |

## Module: App

File: `app/fe/src/App.tsx`

| TC | Description | Input | Expected Output |
|----|-------------|-------|-----------------|
| TC-021 | Root path redirects or renders login page | `path = "/"` | `LoginPage` is rendered |

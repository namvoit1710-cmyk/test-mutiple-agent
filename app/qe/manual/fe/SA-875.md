# Unit Test Cases: SA-875 [FE]

Source spec: `app/docs/spec/SA-875.md`

## Module: Auth Context

File: `app/fe/src/lib/auth-context.tsx`

| TC | Description | Input | Expected Output |
|----|-------------|-------|-----------------|
| TC-001 | `login` sets user and authentication state | `email = "test@example.com", password = "password123"` | `isAuthenticated = true, user = { email: "test@example.com", password: "password123" }` |
| TC-002 | `logout` clears user and authentication state | `isAuthenticated = true, user = { email: "test@example.com", ... }` | `isAuthenticated = false, user = null` |
| TC-003 | `useAuth` throws error when used outside provider | Call `useAuth()` without `AuthProvider` wrapper | Error: `"useAuth must be used within an AuthProvider"` |

## Module: Private Route

File: `app/fe/src/components/private-route.tsx`

| TC | Description | Input | Expected Output |
|----|-------------|-------|-----------------|
| TC-004 | Redirects to login when unauthenticated | `isAuthenticated = false` | `<Navigate to="/login" replace />` |
| TC-005 | Renders children when authenticated | `isAuthenticated = true` | `children` are rendered |

## Module: Auth Route

File: `app/fe/src/components/auth-route.tsx`

| TC | Description | Input | Expected Output |
|----|-------------|-------|-----------------|
| TC-006 | Redirects to home when authenticated | `isAuthenticated = true` | `<Navigate to="/home" replace />` |
| TC-007 | Renders children when unauthenticated | `isAuthenticated = false` | `children` are rendered |

## Module: Password Control

File: `app/fe/src/components/password-control.tsx`

| TC | Description | Input | Expected Output |
|----|-------------|-------|-----------------|
| TC-008 | Initial state is masked | `value = "secret123"` | Input `type="password"`, eye icon visible |
| TC-009 | Toggle shows password | Click toggle button | Input `type="text"`, eye-off icon visible |
| TC-010 | Toggle hides password | Click toggle button while `type="text"` | Input `type="password"`, eye icon visible |
| TC-011 | Disabled state prevents interaction | `disabled = true` | Input and toggle button both have `disabled` attribute |
| TC-012 | Input change triggers onChange callback | Type `"a"` into input | `onChange` called with `"a"` |

## Module: Home Page

File: `app/fe/src/pages/home.tsx`

| TC | Description | Input | Expected Output |
|----|-------------|-------|-----------------|
| TC-013 | Displays welcome greeting with user email | `user = { email: "user@example.com" }` | `"Welcome, user@example.com"` is visible |
| TC-014 | Displays app slogan | `SLOGAN = "Intelligent automation starts here."` | `"Intelligent automation starts here."` is visible |
| TC-015 | Logout button triggers logout function | Click logout button | `logout()` function is called |

## Module: Login Page

File: `app/fe/src/pages/login.tsx`

| TC | Description | Input | Expected Output |
|----|-------------|-------|-----------------|
| TC-016 | Renders login form fields | Initial render | Email input and PasswordControl are visible |
| TC-017 | Validation error for empty email | Submit form with `email = ""` | `"Email is required"` error message visible |
| TC-018 | Validation error for invalid email format | Submit form with `email = "invalid-email"` | `"Invalid email format"` error message visible |
| TC-019 | Validation error for empty password | Submit form with `password = ""` | `"Password is required"` error message visible |
| TC-020 | Successful login calls auth context | Submit `email = "test@example.com", password = "password123"` | `login("test@example.com", "password123")` is called |

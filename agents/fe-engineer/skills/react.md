# React skill

- Functional components only, no class components
- One component per file, file name matches component name
- Use custom hooks to extract logic from components (`use` prefix)
- Keep components under 100 lines — split if larger
- Co-locate styles with components (CSS modules or styled-components)
- Use `React.memo` only when profiling shows a real render problem

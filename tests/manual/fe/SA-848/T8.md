
# Manual Test Cases: SA-848 / T8 [FE]

Source spec: `app/docs/spec/SA-848.md`
Source plan task: `T8` in `app/docs/plan/SA-848.md`

### TC-001: Verify `LoginForm.css` container and form styles

- **Priority:** High
- **Type:** Happy Case
- **Preconditions:** The `app/fe/src/components/LoginForm.css` file exists.
- **Test Data:** 
```css
.login-container {
  display: flex;
  justify-content: center;
  align-items: center;
  min-height: 100vh;
  background-color: #f0f2f5; /* Light gray background */
}

.login-form {
  background-color: #ffffff;
  padding: 2rem;
  border-radius: 8px;
  box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
  max-width: 400px;
  width: 100%;
}
```

| # | Step | Expected Result |
|---|------|-----------------|
| 1 | Open the `app/fe/src/components/LoginForm.css` file. | The file is empty or contains previous content. |
| 2 | Add the CSS rules for `.login-container` and `.login-form` to the file. | The CSS rules for centering the container and styling the form as a card are present. |
| 3 | Verify the `.login-container` styles: `display: flex`, `justify-content: center`, `align-items: center`, `min-height: 100vh`, and `background-color`. | All specified styles for the container are correctly applied. |
| 4 | Verify the `.login-form` styles: `background-color`, `padding`, `border-radius`, `box-shadow`, `max-width`, and `width`. | All specified styles for the form are correctly applied. |

- **Postconditions:** `LoginForm.css` contains the styles for the login container and the form itself.
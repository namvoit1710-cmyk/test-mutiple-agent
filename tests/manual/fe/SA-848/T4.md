
# Manual Test Cases: SA-848 / T4 [FE]

Source spec: `app/docs/spec/SA-848.md`
Source plan task: `T4` in `app/docs/plan/SA-848.md`

### TC-001: Verify `LoginForm.tsx` state management implementation

- **Priority:** High
- **Type:** Happy Case
- **Preconditions:** `LoginForm.tsx` has the basic structure and imports from Task 3.
- **Test Data:** 
```typescript
import React, { useState } from 'react';
import './LoginForm.css';

const LoginForm: React.FC = () => {
  const [username, setUsername] = useState('');
  const [password, setPassword] = useState('');

  // Event handlers will be added in the next task

  return (
    <div className="login-container">
      <form className="login-form">
        <h2>Login</h2>
        <div className="form-group">
          <label htmlFor="username">Username</label>
          <input 
            type="text"
            id="username"
            value={username}
            // onChange will be added in the next task
          />
        </div>
        <div className="form-group">
          <label htmlFor="password">Password</label>
          <input 
            type="password"
            id="password"
            value={password}
            // onChange will be added in the next task
          />
        </div>
        {/* Submit button will be added later */}
      </form>
    </div>
  );
};

export default LoginForm;
```

| # | Step | Expected Result |
|---|------|-----------------|
| 1 | Open the `app/fe/src/components/LoginForm.tsx` file. | The file contains the basic structure and imports from Task 3. |
| 2 | Add the `useState` hooks for `username` and `password` with initial empty string values. | The lines `const [username, setUsername] = useState('');` and `const [password, setPassword] = useState('');` are present in the component. |
| 3 | Update the `value` attribute of the username input to `value={username}`. | The username input is now controlled by the `username` state. |
| 4 | Update the `value` attribute of the password input to `value={password}`. | The password input is now controlled by the `password` state. |

- **Postconditions:** `LoginForm.tsx` includes state management for username and password, and the input fields are wired to these states.

# Manual Test Cases: SA-848 / T9 [FE]

Source spec: `app/docs/spec/SA-848.md`
Source plan task: `T9` in `app/docs/plan/SA-848.md`

### TC-001: Verify `LoginForm.css` form group, label, and input styles

- **Priority:** High
- **Type:** Happy Case
- **Preconditions:** `LoginForm.css` contains styles for container and form (from Task 8).
- **Test Data:** 
```css
.login-container {
  display: flex;
  justify-content: center;
  align-items: center;
  min-height: 100vh;
  background-color: #f0f2f5; /* Light gray background */
}

.login-form {
  background-color: #ffffff;
  padding: 2rem;
  border-radius: 8px;
  box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
  max-width: 400px;
  width: 100%;
}

.form-group {
  margin-bottom: 1rem; /* Spacing between fields */
  display: flex;
  flex-direction: column;
}

.login-form label {
  margin-bottom: 0.5rem;
  font-weight: bold;
  font-size: 0.9rem;
}

.login-form input[type="text"],
.login-form input[type="password"] {
  padding: 0.75rem;
  border: 1px solid #ccc;
  border-radius: 4px;
  font-size: 1rem;
  width: 100%;
  box-sizing: border-box; /* Include padding and border in the element's total width and height */
}
```

| # | Step | Expected Result |
|---|------|-----------------|
| 1 | Open the `app/fe/src/components/LoginForm.css` file. | The file contains styles for container and form. |
| 2 | Add the CSS rules for `.form-group`, `label`, and `input` elements within the `.login-form`. | The CSS rules for form groups, labels, and inputs are added to the file. |
| 3 | Verify the `.form-group` styles: `margin-bottom`, `display: flex`, and `flex-direction: column`. | The specified styles for form groups are correctly applied. |
| 4 | Verify the `label` styles: `margin-bottom`, `font-weight`, and `font-size`. | The specified styles for labels are correctly applied. |
| 5 | Verify the `input` styles: `padding`, `border`, `border-radius`, `font-size`, `width`, and `box-sizing`. | The specified styles for input fields are correctly applied. |

- **Postconditions:** `LoginForm.css` includes styles for form groups, labels, and input fields.

# Manual Test Cases: SA-848 / T10 [FE]

Source spec: `app/docs/spec/SA-848.md`
Source plan task: `T10` in `app/docs/plan/SA-848.md`

### TC-001: Verify `LoginForm.css` button and heading styles

- **Priority:** High
- **Type:** Happy Case
- **Preconditions:** `LoginForm.css` contains styles for container, form, form groups, labels, and inputs (from Task 9).
- **Test Data:** 
```css
.login-container {
  display: flex;
  justify-content: center;
  align-items: center;
  min-height: 100vh;
  background-color: #f0f2f5; /* Light gray background */
}

.login-form {
  background-color: #ffffff;
  padding: 2rem;
  border-radius: 8px;
  box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
  max-width: 400px;
  width: 100%;
}

.form-group {
  margin-bottom: 1rem; /* Spacing between fields */
  display: flex;
  flex-direction: column;
}

.login-form label {
  margin-bottom: 0.5rem;
  font-weight: bold;
  font-size: 0.9rem;
}

.login-form input[type="text"],
.login-form input[type="password"] {
  padding: 0.75rem;
  border: 1px solid #ccc;
  border-radius: 4px;
  font-size: 1rem;
  width: 100%;
  box-sizing: border-box; /* Include padding and border in the element's total width and height */
}

.login-form button {
  background-color: #007bff; /* Primary blue */
  color: white;
  padding: 0.75rem;
  border: none;
  border-radius: 4px;
  font-size: 1rem;
  cursor: pointer;
  width: 100%;
  margin-top: 1rem;
  transition: background-color 0.2s ease;
}

.login-form button:hover {
  background-color: #0056b3; /* Darker blue on hover */
}

.login-form h2 {
  margin-bottom: 1.5rem;
  text-align: center;
  color: #333;
}
```

| # | Step | Expected Result |
|---|------|-----------------|
| 1 | Open the `app/fe/src/components/LoginForm.css` file. | The file contains styles for container, form, form groups, labels, and inputs. |
| 2 | Add the CSS rules for `button` and `h2` elements within the `.login-form`. | The CSS rules for the button and heading are added to the file. |
| 3 | Verify the `button` styles: `background-color`, `color`, `padding`, `border`, `border-radius`, `font-size`, `cursor`, `width`, `margin-top`, and `transition`. | The specified styles for the button are correctly applied. |
| 4 | Verify the `button:hover` styles: `background-color`. | The hover effect for the button is correctly applied. |
| 5 | Verify the `h2` styles: `margin-bottom`, `text-align`, and `color`. | The specified styles for the heading are correctly applied. |

- **Postconditions:** `LoginForm.css` includes styles for the submit button and the form heading.
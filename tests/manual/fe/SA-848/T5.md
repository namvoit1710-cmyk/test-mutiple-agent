
# Manual Test Cases: SA-848 / T5 [FE]

Source spec: `app/docs/spec/SA-848.md`
Source plan task: `T5` in `app/docs/plan/SA-848.md`

### TC-001: Verify `LoginForm.tsx` event handlers implementation

- **Priority:** High
- **Type:** Happy Case
- **Preconditions:** `LoginForm.tsx` has state management and input fields wired to state (from Task 4).
- **Test Data:** 
```typescript
import React, { useState, FormEvent, ChangeEvent } from 'react';
import './LoginForm.css';

const LoginForm: React.FC = () => {
  const [username, setUsername] = useState('');
  const [password, setPassword] = useState('');

  const handleUsernameChange = (e: ChangeEvent<HTMLInputElement>) => {
    setUsername(e.target.value);
  };

  const handlePasswordChange = (e: ChangeEvent<HTMLInputElement>) => {
    setPassword(e.target.value);
  };

  const handleSubmit = (e: FormEvent<HTMLFormElement>) => {
    e.preventDefault();
    alert(`Username: ${username}, Password: ${password}`);
  };

  return (
    <div className="login-container">
      <form className="login-form" onSubmit={handleSubmit}>
        <h2>Login</h2>
        <div className="form-group">
          <label htmlFor="username">Username</label>
          <input 
            type="text"
            id="username"
            value={username}
            onChange={handleUsernameChange}
          />
        </div>
        <div className="form-group">
          <label htmlFor="password">Password</label>
          <input 
            type="password"
            id="password"
            value={password}
            onChange={handlePasswordChange}
          />
        </div>
        <button type="submit">Login</button>
      </form>
    </div>
  );
};

export default LoginForm;
```

| # | Step | Expected Result |
|---|------|-----------------|
| 1 | Open the `app/fe/src/components/LoginForm.tsx` file. | The file contains state management and input fields wired to state. |
| 2 | Implement `handleUsernameChange` function to update `username` state. | The `handleUsernameChange` function is defined and correctly updates the `username` state using `setUsername`. |
| 3 | Implement `handlePasswordChange` function to update `password` state. | The `handlePasswordChange` function is defined and correctly updates the `password` state using `setPassword`. |
| 4 | Implement `handleSubmit` function to prevent default form submission and display an alert. | The `handleSubmit` function is defined, calls `e.preventDefault()`, and displays an alert with the format `Username: [username], Password: [password]`. |
| 5 | Add `onChange={handleUsernameChange}` to the username input. | The username input correctly calls `handleUsernameChange` when typed into. |
| 6 | Add `onChange={handlePasswordChange}` to the password input. | The password input correctly calls `handlePasswordChange` when typed into. |
| 7 | Add `onSubmit={handleSubmit}` to the form element. | The form correctly calls `handleSubmit` when the submit button is clicked. |

- **Postconditions:** `LoginForm.tsx` has all necessary event handlers implemented and wired to the respective elements.